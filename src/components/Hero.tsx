'use client';

import React from 'react';

function Hero(){
    return (
        <section className="bg-hero-pattern h-screen flex items-center justify-center">
            <h1 className="text-5xl font-bold text-white">Welcome to My Application</h1>
        </section>
    );
}

export default Hero;